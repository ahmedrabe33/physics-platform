apiVersion: v1
kind: Service
metadata:
  name: progress-service
  namespace: physics

spec:
  selector:
    app: progress-service

  ports:
    - name: http
      port: 3004
      targetPort: 3004

  type: ClusterIP
